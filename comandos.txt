docker build -t jupyter-ubuntu .
docker run -p 8888:8888 -v $(pwd):/app jupyter-ubuntu


pip install -r requirements.txt



IDEIAS:

Com o TSP eu vou pegar todos os pontos a serem visitados e a ordem desses pontos
Para cada ponto, eu vou calcular a curva de dubins de um ponto X para um ponto Y.
